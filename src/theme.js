import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#2a9d8f',
      light: '#4fb3a9',
    },
    secondary: {
      main: '#264653',
    },
    success: {
      main: '#34a853',
    },
    error: {
      main: '#dc3545',
    },
  },
});
